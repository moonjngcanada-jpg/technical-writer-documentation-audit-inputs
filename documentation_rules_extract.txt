Documentation Audit Rules Extract (fictional)

R-1 Active document scope. Active documentation outputs include only rows from doc_inventory_register.csv where doc_status=Active and duplicate_of is blank. DOC-2026-809 is a Closed duplicate of DOC-2026-806 and must be excluded from active document workbooks. Duplicate-only evidence must still be represented in source rejection and validation outputs.

R-2 Source hierarchy. documentation_rules_extract.txt controls the audit rules. product_change_log.csv controls accepted product changes. current accepted SME notes in sme_interview_notes.txt control product meaning when they agree with product_change_log.csv. api_reference_extract.csv controls API evidence only after CSV-integrity review. ui_string_export.csv controls current UI label evidence only after CSV-integrity review. screenshot_alt_text_log.csv controls alt-text evidence. localization_review_notes.csv controls localization readiness. release_note_draft.csv controls release-note draft state but does not override accepted product changes, current accepted SME notes, terminology_glossary.csv, deprecation_schedule.json, or CSV-integrity findings. doc_inventory_register.csv is intake/planning only and does not control final readiness, release coverage, terminology compliance, API status, localization readiness, or validation outcome.

R-3 CSV integrity. Inspect physical CSV rows with a CSV-aware parser before conclusions. Rows with fewer fields than the header are malformed or missing-field evidence. Rows with extra unquoted delimiters may be reconstructable only when the extra pieces belong to the final text field; reconstructable evidence remains limited-use and must be disclosed. Valid quoted commas are clean CSV syntax. Duplicate-only rows are source-limitation evidence even when they parse cleanly. JSON and TXT files are not CSV files.

R-4 Status precedence. Use Blocked when an active doc has malformed controlling API evidence, unresolved deprecation banner requirement, restricted/draft-only release state for required customer-visible content, or a source limitation that prevents release verification. Use Needs revision when evidence is trusted but content must be corrected for terminology, alt text, localization, or release-note coverage. Use Ready only when source hierarchy, product changes, terminology, API evidence, alt text, localization, release notes, duplicate handling, and CSV-integrity checks are clear.

R-5 Fee-like arithmetic not applicable. Do not invent financial calculations. This task is about documentation artifacts only.

R-6 API/deprecation. For DOC-2026-803, examples must use /v2/tokens and may mention /v1/token only as deprecated alias. For DOC-2026-805, /v1/metering must show sunset date 2026-09-30 and replacement /v2/meters. Malformed API rows tied to DOC-2026-805 keep API evidence verification-dependent.

R-7 UI terminology. DOC-2026-806 must use Saved filters as customer-facing terminology. Advanced filters is old terminology when used as current UI label, title, alt text, release summary, or localization term. UI-806 has a reconstructable extra-delimiter help_text row and remains limited-use evidence.

R-8 Localization. accepted localization can support readiness. pending, blocked, draft, or duplicate_only localization cannot make a doc release-ready. DOC-2026-807 has only draft SME/localization support for the staged rollout prerequisite and needs revision unless accepted evidence appears.

R-9 Source rejection. source_rejection_register.xlsx must contain distinct rows for material rejected, limited, non-controlling, lower-authority, draft-only, duplicate-only, malformed, missing-field, reconstructable extra-delimiter, valid-quoted-comma-clean, contradicted, and stale terminology evidence when supported by the files. Do not collapse distinct source issues into a single generic row.

R-10 Validation. doc_integrity_validation_report.xlsx must verify workbook delivery, worksheet names, active/duplicate handling, CSV-integrity findings, clean CSV file classification, terminology handling, API/deprecation handling, release-note coverage, localization readiness, source-rejection coverage, and final validation status. final_validation_status observed value must be pass_with_open_items only if artifacts are structurally correct and unresolved doc issues are intentionally open; otherwise observed must be fail.
